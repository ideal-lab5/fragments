[package]
name = "fa_nft"
version = "0.1.0"
authors = ["juan@idealabs.network"]
edition = "2021"

[dependencies]
ink = { version = "5.0.0", default-features = false }

[lib]
path = "lib.rs"

[features]
default = ["std"]
std = [
    "ink/std",
]
ink-as-dependency = []
